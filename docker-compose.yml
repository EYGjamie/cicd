services:
  myapp:
    image: ghcr.io/EYGjamie/myapp:${IMAGE_TAG}
    restart: unless-stopped
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 10s
      timeout: 2s
      retries: 5
